# Deployment

The production container and compose deployment belong to M2. This directory is reserved for
those artifacts so deployment concerns remain separate from the backend and frontend builds.

M0/M1 local development uses Quarkus Dev Services to start the pinned PostgreSQL image described
in `docs/DEVELOPMENT.md`.
